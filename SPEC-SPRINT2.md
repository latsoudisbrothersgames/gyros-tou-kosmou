# Γύρος του Κόσμου — Sprint 2 (22.09.2026): τρία νέα modes

Βάση: το main μετά το Sprint 1 (PWA offline, `src/reactions/`, 13 διαθέσεις CountryBall, SpeechBubble, haptics, probes).
Διάβασε πρώτα: `IDEAS-2026-09-22.md` §2, `SPEC-SPRINT1.md`, `CODEX_REPORT.md`, `src/types/game.ts`, `src/game/questionGenerator.ts`,
`src/game/scoring.ts`, `src/hooks/useGameSession.ts`, `src/pages/GamesPage.tsx`, `src/pages/QuizGamePage.tsx`, `src/pages/ScratchGamePage.tsx`
(πρότυπο για mode με δική του σελίδα), `src/components/GameResults/*`, `src/reactions/*`, `src/data/countries.ts`, `src/types/country.ts`.

## Απαρέγκλιτοι κανόνες
1. Δουλεύει στο κινητό παντού χωρίς σύνδεση: κανένα νέο asset εκτός build, όλα στο precache, `npm run probe:offline` συνεχίζει να περνά (πρόσθεσε τις νέες διαδρομές στη λίστα οθονών του probe).
2. **Τίποτα δεν αποκαλύπτει τη λύση πριν την απάντηση** (κανόνας Apollo 22.09): καμία μπάλα με σημαία, καμία σημαία, κανένα όνομα δίπλα σε επιλογή πριν απαντήσει ο παίκτης. Μετά την απάντηση εμφανίζονται και αντιδρούν.
3. Οι φούσκες δείχνουν πάντα ολόκληρη την πρόταση (SpeechBubble ως έχει).
4. Ελληνικό UI, μεγάλα touch targets (≥ 44px), portrait iPhone 390×844 ως πρώτη οθόνη σχεδίασης. Ήχοι μόνο WebAudio (soundManager). Σεβασμός «Λιγότερη κίνηση».
5. Branch `codex/sprint2-modes` (ή κλώνος /private/tmp/gyros-sprint2 αν το .git είναι μόνο για ανάγνωση), commit ανά mode, ποτέ στο main. Καμία νέα εξάρτηση.

## Κοινά για τα νέα modes
- Νέα `GameModeId`: `'whoami' | 'parade' | 'bigger'` με ετικέτες «Ποιος είμαι;», «Παρέλαση Σημαιών», «Ποια είναι η μεγαλύτερη;» στο `GAME_MODE_LABELS`, κάρτες στο `GamesPage` (με emoji και μία γραμμή περιγραφής), διαδρομές `/play/whoami`, `/play/parade`, `/play/bigger` με δικές τους σελίδες (πρότυπο ScratchGamePage), δυσκολία και μήκος όπως τα υπάρχοντα.
- Σκορ μέσω `scoring.ts` (μην αλλάξεις τους υπάρχοντες τύπους), εγγραφή στον πίνακα βαθμολογίας με το νέο mode, Συλλογή (`addToCollection`) για κάθε σωστή χώρα, `ExplorerPassport` και `GameResults` στο τέλος όπως στα άλλα.
- Αντιδράσεις: εκπέμπονται τα υπάρχοντα γεγονότα (`question:new`, `answer:correct`, `answer:wrong`, `game:end`) ώστε οι μπάλες να αντιδρούν με τους κανόνες του Sprint 1. Πρόσθεσε νέα γεγονότα μόνο όπου χρειάζεται (βλ. παρακάτω), με tests στο `test:reactions`.

## Mode 1 — «Ποιος είμαι;» (`whoami`)
- Στη μέση μία μεγάλη CountryBall **χωρίς τη σημαία της**: σκούρα γκρίζα «σιλουέτα» με το ίδιο ντετερμινιστικό πρόσωπο (μάτια, στόμα) και διάθεση `thinking`. Η σημαία αποκαλύπτεται μόνο μετά την απάντηση (animation «ξεθώριασμα» της σιλουέτας).
- Τρεις σταδιακές ενδείξεις, μία κάθε φορά με κουμπί «Δώσε μου ένδειξη»: (1) ήπειρος, (2) το αρχικό γράμμα της πρωτεύουσας και ο πληθυσμός στρογγυλεμένος (`formatPopulationGreek`), (3) ένα `factsGreek` που ΔΕΝ περιέχει το όνομα της χώρας ή της πρωτεύουσας (φίλτραρε· αν δεν υπάρχει τέτοιο, δώσε το νόμισμα ή τη γλώσσα). Η μπάλα «λέει» κάθε ένδειξη με φούσκα.
- Τέσσερις επιλογές ονομάτων χωρών (χρησιμοποίησε `generateCountryDistractors`) χωρίς μπάλες/σημαίες μέχρι την απάντηση (κανόνας 2). Πόντοι: 100 χωρίς ένδειξη, 70 με μία, 45 με δύο, 25 με τρεις, + μπόνους σερί όπως στο `scoring.ts` (πρόσθεσε καθαρή συνάρτηση `scoreWithHints(hintsUsed, streak)` με tests).
- Μετά την απάντηση: αποκάλυψη σημαίας, `celebrate` ή `shrug`/`wave` κατά τους κανόνες, φούσκα «Ναι! Είμαι η/ο/το …» ή «Όχι… ήμουν η/ο/το …» (χρήση `nameGreekAccusative`/άρθρου όπως στο ballLines).

## Mode 2 — «Παρέλαση Σημαιών» (`parade`)
- Πάνω: «Βρες: {όνομα χώρας}». Από δεξιά προς αριστερά περνούν 3–5 CountryBalls (με σημαίες — εδώ η σημαία ΔΕΝ είναι η λύση, το όνομα είναι), σε δύο λωρίδες, με CSS animation `translateX` (όχι JS ανά καρέ). Ο παίκτης πατά τη σωστή πριν βγει από την οθόνη.
- Δυσκολία ρυθμίζει ταχύτητα και πλήθος: εύκολο 3 μπάλες/9΄΄ διέλευση, μεσαίο 4/7΄΄, δύσκολο 5/5΄΄· κάθε 3 σωστές η ταχύτητα +10% (όριο 2×).
- Πάτημα σωστής: `celebrate`, +πόντοι (100 + μπόνους ταχύτητας ανάλογα με το πόσο νωρίς), σταματούν όλες για 0,8΄΄ και φεύγει ο γύρος. Πάτημα λάθους: `shrug` στη λάθος, `wave` στη σωστή, 0 πόντοι, ο γύρος τελειώνει. Αν βγει η σωστή από την οθόνη χωρίς πάτημα: μετρά ως λάθος, η σωστή λέει «Έφυγα!».
- Πρόσθεσε γεγονός `parade:miss {iso2}` (η σωστή που ξέφυγε → `sad`). «Λιγότερη κίνηση»: αντί για κύλιση, οι μπάλες εμφανίζονται ακίνητες σε σειρά με χρονόμετρο κύκλου.
- Χρησιμοποίησε `pointerdown` για άμεση απόκριση στο κινητό· touch targets ≥ 56px· χωρίς επικαλύψεις μπαλών.

## Mode 3 — «Ποια είναι η μεγαλύτερη;» (`bigger`)
- Δύο χώρες αντιμέτωπες, ΜΟΝΟ με ονόματα και σιλουέτες μπαλών (σημαίες μετά την απάντηση, κανόνας 2). Ερώτηση εναλλάξ: «Ποια έχει περισσότερους κατοίκους;» / «Ποια έχει μεγαλύτερη έκταση;». Χρησιμοποίησε `population` / `areaKm2`· απόκλεισε ζεύγη με διαφορά < 15% (πολύ κοντινά) και χώρες χωρίς δεδομένα.
- Μετά την απάντηση: αποκαλύπτονται οι δύο τιμές (μορφοποιημένες στα ελληνικά: `formatPopulationGreek`, έκταση με τελείες χιλιάδων και «km²»), η μεγαλύτερη μπάλα «φουσκώνει» (scale 1.25) με `proud`, η μικρή `shy`. Σωστό/λάθος με τους γενικούς κανόνες.
- Δυσκολία: εύκολο = tier 1 και διαφορά ≥ 3×, μεσαίο = tier 1–2 και ≥ 1,5×, δύσκολο = όλα και ≥ 1,15×. Πόντοι όπως το βασικό `scoring.ts`.

## QA (πριν τελειώσεις)
- `npm run build`, `npm run lint`, `npx tsc -b`, `npm run test:reactions` (με τα νέα tests), `npm run test:engine`, `npm run test:data`, `npm run audit:dist` καθαρά.
- `npm run probe:offline` PASS με τις τρεις νέες διαδρομές προστεθειμένες στη λίστα οθονών (και μία απάντηση σε καθεμία).
- Νέο `tools/modes_probe.mjs` (`npm run probe:modes`), Chrome 390×844: για κάθε mode παίζει 3 γύρους (βρίσκει τη σωστή απάντηση από τα δεδομένα μέσω `data-*` attributes που ΔΕΝ είναι ορατά στον παίκτη — π.χ. `data-answer` μόνο ΜΕΤΑ την απάντηση, ή από το `focus` param όπως το balls probe), ελέγχει ότι **πριν την απάντηση δεν υπάρχει καμία σημαία/μπάλα-σημαία της λύσης στο DOM** (κανόνας 2), ότι οι κλάσεις διαθέσεων εμφανίζονται μετά, και τραβά screenshots `tools/shots/mode-whoami.png`, `mode-parade.png`, `mode-bigger.png` (πριν και μετά την απάντηση).
- `CODEX_REPORT.md`: τι έγινε ανά mode, αρχεία, αποφάσεις, τι έμεινε.
