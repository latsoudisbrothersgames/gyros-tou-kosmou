# Sprint 1 — Αναφορά Codex

**Κατάσταση: Tasks 1–4 υλοποιημένα. QA browser/offline μη ολοκληρωμένο λόγω περιορισμού εκκίνησης Chrome. Δεν δηλώνεται offline PASS.**

## Χώρος εργασίας

Το αρχικό `.git` είναι μόνο για ανάγνωση σύμφωνα με το sandbox. Έγινε τοπικός κλώνος (μαζί με το commit του spec) στο `/private/tmp/gyros-sprint1`, branch `codex/sprint1-balls`. Καμία αλλαγή ή commit στο main. Το npm χρησιμοποιεί cache `/private/tmp/gyros-npm-cache` λόγω περιορισμών εγγραφής στην προσωπική cache.

## Task 1

Πλήρες precache με `vite-plugin-pwa`, ελληνικό manifest, σωστό base και PNG 192/512 από το υπάρχον εικονίδιο με sips. Ένδειξη offline στην αρχική και ανανέωση με επιλογή του παίκτη (`onNeedReload`, διατηρώντας `registerType: autoUpdate`). Η σημαία της Ελλάδας στο λογότυπο λειτουργεί και ως κοινός οπτικός έλεγχος offline στις οθόνες που δεν έχουν δικές τους σημαίες.

Αρχεία: `vite.config.ts`, `index.html`, `public/icon-*.png`, `src/pwa/*`, αρχική/πλοήγηση, `tools/probe_helpers.mjs`, `tools/offline_probe.mjs`, package/lock.

Το probe χτίζει, ξεκινά/κλείνει preview, ελέγχει κάθε asset στην Cache Storage, κόβει δίκτυο πριν τις εσωτερικές οθόνες, ελέγχει σημαίες/σφάλματα/εξωτερικά αιτήματα και απαντά στο quiz. Build και lint επιτυχή. Η πρώτη εκτέλεση του probe μπλοκαρίστηκε στην εκκίνηση Chrome (SIGABRT μέσα στο sandbox)· επανέλεγχος στο τελικό QA.

## Task 2

Προστέθηκαν `src/reactions/{events,rules,ReactionsProvider,useBallReaction,useQuizReactions}`: event bus με replay για μπάλες που μόλις εμφανίζονται, ακυρώσιμη ουρά διαθέσεων και προτεραιότητα του ρητού `mood`. Ο τύπος των 13 διαθέσεων προστέθηκε εδώ ως προϋπόθεση των κανόνων· τα γραφικά ακολουθούν στο Task 3.

Συνδέθηκαν οι πηγές σε `useGameSession`, Quiz/Map/Country/Collection και GameResults. Οι επιλογές του quiz έχουν μόνιμες μικρές μπάλες, τα αποτελέσματα παρέλαση όλων των χωρών και η συλλογή ξεχωριστό κουμπί αντίδρασης και σύνδεσμο ονόματος. Ο χάρτης απέκτησε μόνο `data-iso2` για μέτρηση απόστασης στο μετασχηματισμένο κέντρο SVG· δεν άλλαξαν προβολή, χώρες ή βαθμολογία.

Απόφαση: δεν υπήρχε χρονόμετρο λήξης. Το `timer:low` χρησιμοποιεί τα τελευταία 2 δευτερόλεπτα του υπάρχοντος παραθύρου μπόνους ταχύτητας (10 s), χωρίς χρονικό περιορισμό απάντησης. Αδράνεια 10 s από την τελευταία είσοδο.

`npm run test:reactions`: PASS 23 συνδυασμοί και επιπλέον διάρκειες/ουρές. `npm run build`: PASS. Τα tests τρέχουν με την εγγενή υποστήριξη TypeScript του Node 24, χωρίς νέα εξάρτηση.

## Task 3

Κάθε νέα διάθεση απέκτησε δικό της CSS animation και εκφράσεις SVG, κρατώντας τον ντετερμινιστικό χαρακτήρα ανά iso2. Νέα στοιχεία: βλέφαρα, φρύδια, χέρια, ιδρώτας, ζζζ, κομφετί. Κοινός `IntersectionObserver` (`src/reactions/liveBalls.ts`) δίνει έως 8 θέσεις σε ορατές μπάλες· οι άλλες δεν τρέχουν animations ή ουρές. Οι θέσεις αναπληρώνονται στην κύλιση/αποπροσάρτηση.

Ένας listener pointermove και ένας touchmove στον provider, κοινό requestAnimationFrame, ανάγνωση γεωμετρίας πριν από εγγραφή CSS variables, μετατόπιση κορών έως 3px χωρίς React render ανά κίνηση. Το προτιμώμενο μειωμένο motion του συστήματος και η νέα αποθηκεύσιμη ρύθμιση απενεργοποιούν κινήσεις/μάτια/κομφετί, κρατώντας τις εκφράσεις. Το κουμπί ρύθμισης ακολουθεί στο Task 4.

Build PASS. Προστέθηκαν επιτυχείς έλεγχοι ορίου 8, αναπλήρωσης θέσης, μήκους μετατόπισης και reduced motion στο `test:reactions`.

## Task 4

`SpeechBubble` πάνω από τις μπάλες: έως 2 γραμμές, πλήρες κείμενο για βοηθητικές τεχνολογίες/title, 2,5 s ανά ατάκα. Στη χώρα ακολουθεί τον χαιρετισμό ένα quick fact. `src/data/ballLines.ts`: 6 διαφορετικές ελληνικές ατάκες για καθεμία από τις 13 διαθέσεις, ντετερμινιστική εναλλαγή και άρθρο από την υπάρχουσα αιτιατική. Αφαιρέθηκε το αγγλικό όνομα από το UI της χώρας, σύμφωνα με τον κανόνα ελληνικού UI, χωρίς αλλαγή δεδομένων.

Νέο WebAudio `blip` ανά 3 χαρακτήρες, με τον υπάρχοντα διακόπτη ήχου. `src/utils/haptics.ts`: ασφαλές προαιρετικό vibrate, 35ms για σωστό και [25,60,25] για λάθος. Οι ρυθμίσεις ήχου, δόνησης (αρχικά ενεργή) και λιγότερης κίνησης βρίσκονται στο γρανάζι της πλοήγησης και αποθηκεύονται με συμβατότητα παλιών ρυθμίσεων.

Build και tests PASS: 78 ατάκες, εναλλαγή, άρθρα, facts/fallback, δονήσεις/απουσία API. Δεν εγκαταστάθηκε καμία εξάρτηση πέρα από τα δύο επιτρεπόμενα dev packages.

## Τελικό QA

- `npm run build`: PASS, χωρίς warnings. Το όριο προειδοποίησης chunk έγινε 800 KB επειδή ο υπάρχων τοπικός άτλαντας είναι ~756 KB και πρέπει να μένει offline. Καμία αλλαγή στον άτλαντα.
- `npm run lint`: PASS χωρίς warnings. Στοχευμένη allowlist για τις υπάρχουσες κοινές εξαγωγές βοηθημάτων/contexts στο Fast Refresh και σχόλιο εξαίρεσης για τη σκόπιμη αφαίρεση control characters στο όνομα. Οι υπόλοιποι κανόνες παραμένουν ενεργοί.
- `npx --no-install tsc -b`: PASS.
- `npm run test:reactions`: PASS (όλοι οι κανόνες, διάρκειες/ουρές, replay/unsubscribe/clear, όριο 8, μετατόπιση ματιών, reduced motion, 78 ατάκες, άρθρα/facts, haptics, αντιστοίχιση πρωτεύουσας→ISO).
- `npm run test:engine`: ENGINE OK. Η λογική βαθμολογίας και η παραγωγή ερωτήσεων παραμένουν αμετάβλητες.
- `npm run test:data`: OK, 197 χώρες, καμία σημαία που λείπει. Δεν τροποποιήθηκαν τα δεδομένα χωρών.
- `npm run audit:dist`: PASS, περίπου **3,74 MB** (< 5.000.000 bytes), 88 μοναδικά precached αρχεία από τα 90 αρχεία dist. Τα άλλα δύο είναι ο ίδιος ο SW και το Workbox script, που αποθηκεύονται ως πόροι του service worker από τον browser. Έλεγχος πραγματικών PNG 192/512, manifest και fallback base.
- HTTP audit στο preview: PASS, και τα 90 αρχεία επέστρεψαν HTTP 200 με bytes ακριβώς ίδια με το dist, στο `/gyros-tou-kosmou/`.
- `git diff --check`: PASS. Στον ίδιο τον χάρτη η μόνη διαφορά είναι ένα `data-iso2` attribute. Το αρχικό main παραμένει καθαρό και αμετάβλητο.

### Browser probes — μπλοκαρισμένα, όχι PASS

Εκτελέστηκαν `npm run probe:offline` και `npm run probe:balls` με `playwright-core`, `channel: 'chrome'`, headless και viewport 390×844. Το build ολοκληρώθηκε, αλλά ο εγκατεστημένος Chrome 153 τερματίστηκε με `SIGABRT` κατά το macOS `RegisterApplication`, πριν δημιουργηθεί browser page. Τα διαγνωστικά είναι στα `tools/qa/offline-environment.txt` και `tools/qa/balls-environment.txt`. Οι preview servers έκλεισαν μετά την αποτυχία (καμία διεργασία στην 4173).

Δεν υπήρχε διαθέσιμος browser connector. Το Computer Use επέστρεψε `Computer Use was not approved to use Google Chrome`. Και ο ήδη εγκατεστημένος WebKit απέτυχε στην εκκίνηση μέσα στο ίδιο περιβάλλον. Δεν επιχειρήθηκε αλλαγή δικαιωμάτων ή παράκαμψη sandbox.

Συνεπώς **δεν έχουν επαληθευτεί** ακόμη: εγκατάσταση/έλεγχος SW σε πραγματικό browser, επαναφόρτωση offline, αλληλεπιδράσεις/χρονοσειρές CSS στο κινητό, screenshots και αναβάθμιση από προηγούμενο SW. Τα scripts έχουν ολοκληρωθεί, αλλά δεν υποκαθιστούν επιτυχημένη εκτέλεση. Το `tools/shots/` περιέχει μόνο την εξήγηση της εκκρεμότητας, όχι πλαστά screenshots.

### Διορθώσεις που προέκυψαν στο QA

- Ρητό `--base /gyros-tou-kosmou/` στον preview των probes, αντί να βασίζεται στο build-only base του config.
- Όριο αναμονής για ενεργοποίηση SW και έλεγχος πραγματικής αλλαγής `data-question-id` στο offline quiz.
- Κοινός καθαρισμός replay στην αλλαγή διαδρομής: προηγούμενη αντίδραση δεν μεταφέρεται στην αρχική.
- Επιλογές πρωτεύουσας: αντιστοίχιση `cap-xN` μέσω του υπάρχοντος ονόματος πρωτεύουσας, χωρίς τροποποίηση generator/δεδομένων. Οι μπάλες αυτών των επιλογών εμφανίζονται μετά την απάντηση ώστε να μην αποκαλύπτουν τη λύση με τη σημαία τους.
- Οι νέες διαθέσεις ξεκινούν αμέσως, χωρίς την τυχαία καθυστέρηση του idle. Η μετατόπιση ματιών λαμβάνει υπόψη και την κλίμακα SVG.
- Φούσκες που προσαρμόζονται στα άκρα της οθόνης και πλοήγηση με κύλιση σε πολύ στενά κινητά. Αυτές οι οπτικές προσαρμογές χρειάζονται ακόμη browser QA.

### Αποκλίσεις και όρια

1. Το απαραίτητο `probe:offline PASS` και τα screenshots εκκρεμούν λόγω του παραπάνω αποκλεισμού περιβάλλοντος. Αυτό είναι ουσιαστικό κριτήριο αποδοχής, όχι προαιρετικός έλεγχος.
2. Ο έλεγχος `https://` βρίσκει τέσσερα διαγνωστικά URLs μέσα στις βιβλιοθήκες React/Router/Workbox: `react.dev/errors/`, `reactrouter.com/en/main/routers/picking-a-router.`, `bit.ly/wb-precache`, `github.com/ungap/url-search-params.`. Είναι κείμενα σφαλμάτων/σχόλια, δεν χρησιμοποιούνται για fetch ή φόρτωση assets. Καταγράφονται ρητά αντί να τροποποιηθούν τρίτες βιβλιοθήκες. Ο κυριολεκτικός κανόνας μηδενικών URLs σε strings δεν ικανοποιείται· ο κανόνας μηδενικών εξωτερικών αιτημάτων επιβάλλεται από το offline probe όταν μπορέσει να εκτελεστεί.
3. Η ενημέρωση PWA παραμένει `autoUpdate`, αλλά το `onNeedReload` κρατά την ανανέωση σε κουμπί για να μην διακόπτει συνεδρία. Βασίστηκε στο εγκατεστημένο plugin και στην [επίσημη τεκμηρίωση ενημερώσεων](https://vite-pwa-org.netlify.app/guide/auto-update).
4. Το όριο `timer:low` αφορά το υπάρχον μπόνους ταχύτητας, όχι νέο χρονικό όριο ερώτησης.
5. Δεν έγινε push, merge ή deployment. Η δημόσια διεύθυνση δεν περιέχει ακόμη αυτές τις αλλαγές.

## Πώς επαναλαμβάνονται οι έλεγχοι

Απαιτείται Node 24 και εγκατεστημένο Google Chrome σε περιβάλλον που επιτρέπει την εκκίνησή του:

```sh
cd /private/tmp/gyros-sprint1
npm ci --cache /private/tmp/gyros-npm-cache
npm run build
npm run lint
npx --no-install tsc -b
npm run test:reactions
npm run test:engine
npm run test:data
npm run audit:dist
npm run probe:offline
npm run probe:balls
```

Τα probes χτίζουν μόνα τους και διαχειρίζονται τον preview server. Το balls probe παίζει ολόκληρη συνεδρία, ελέγχει σωστό/λάθος/σερί, χαιρετισμό→fact→εξαφάνιση, συλλογή, όριο 8, μάτια και αποθήκευση ρυθμίσεων. Τα screenshots γράφονται στο `tools/shots/balls-*.png`.

## Δοκιμή σε iPhone

Μετά από δημοσίευση αυτών των commits και επιτυχημένα probes:

1. Άνοιξε με σύνδεση στο Safari το https://latsoudisbrothersgames.github.io/gyros-tou-kosmou/.
2. Περίμενε την ένδειξη «Λειτουργεί χωρίς σύνδεση ✓».
3. Κοινή χρήση → «Προσθήκη στην αρχική οθόνη» → «Προσθήκη».
4. Άνοιξε την εφαρμογή από το νέο εικονίδιο μία φορά με σύνδεση και επιβεβαίωσε πάλι την ένδειξη.
5. Κλείσε την εφαρμογή. Ενεργοποίησε λειτουργία πτήσης και βεβαιώσου ότι το Wi‑Fi είναι κλειστό. Το Mac μπορεί να είναι κλειστό.
6. Άνοιξε από την αρχική οθόνη και παίξε quiz μέχρι τα αποτελέσματα. Άνοιξε χάρτη, εγκυκλοπαίδεια, Ελλάδα και συλλογή. Όλες οι σημαίες/χάρτες πρέπει να φαίνονται.
7. Από το γρανάζι έλεγξε ήχο, δονήσεις όπου υποστηρίζονται και «Λιγότερη κίνηση». Κλείσε/άνοιξε και έλεγξε ότι διατηρούνται.
8. Με σύνδεση και νέα δημοσιευμένη έκδοση έλεγξε το «Νέα έκδοση, πάτα για ανανέωση» και ότι δεν χάνεται η τρέχουσα συνεδρία πριν το πάτημα.

## Commits

- `74ad4ee` — Task 1: PWA και offline probe.
- `5e995b6` — Task 2: μηχανή αντιδράσεων.
- `d7bd0f0` — Task 3: διαθέσεις, μάτια, όριο οκτώ.
- `ce11fb5` — Task 4: ομιλία, ήχος, δονήσεις, ρυθμίσεις.
- Ξεχωριστό τελικό commit QA με τις διορθώσεις, probes, tests και την παρούσα αναφορά.
