# Γύρος του Κόσμου — Sprint 3 (23.09.2026): γεωγραφία σχέσεων

Βάση: το main μετά το Sprint 2 (12 modes μετά από αυτό το sprint· σήμερα 9). Το Sprint 3 του `IDEAS-2026-09-22.md` §3 **ακυρώνεται**
και αντικαθίσταται από τρεις νέες ιδέες του Apollo. Κοινός στόχος: ο παίκτης μαθαίνει **σχέσεις** ανάμεσα σε χώρες
(σύνορα, διαδρομές, σχετικές θέσεις, σχήματα) — όχι άλλη μία ερώτηση «ποια χώρα είναι αυτή».

Διάβασε πρώτα: `GAME-OVERVIEW.md` (πλήρης εικόνα + απαρέγκλιτοι κανόνες), `SPEC-SPRINT2.md`, `CODEX_REPORT.md`, `src/types/game.ts`,
`src/game/newModes.ts`, `src/game/scoring.ts`, `src/pages/NewModeLayout.tsx` + `WhoAmIGamePage.tsx` (πρότυπο νέου mode),
`src/components/WorldMap/*` (ιδίως `useWorldTopology.ts`, `projectToMap`), `src/components/GeoIdentity/*` (σιλουέτα χώρας),
`src/components/CountryBall/*`, `src/reactions/*`, `src/data/countries.ts`, `tools/modes_probe.mjs`, `tools/offline_probe.mjs`.

## Απαρέγκλιτοι κανόνες (ίδιοι με πάντα)
1. Δουλεύει στο κινητό παντού χωρίς σύνδεση: κανένα asset εκτός build, όλα στο precache, `probe:offline` περνά με τις νέες διαδρομές.
2. **Τίποτα δεν προδίδει τη λύση πριν την απάντηση** — ούτε οπτικά, ούτε στο DOM (κανένα `data-*`/κλάση που διαρρέει τη λύση πριν τον έλεγχο).
   Ανά mode αναλυτικά παρακάτω.
3. Οι φούσκες δείχνουν πάντα ολόκληρη την πρόταση.
4. Ελληνικό UI, touch targets ≥ 44px, portrait iPhone 390×844 πρώτα, ήχοι μόνο WebAudio (`soundManager`), σεβασμός «Λιγότερη κίνηση»
   (κρατά εκφράσεις και αποτέλεσμα, κόβει κυλίσεις/πτήσεις — αντί για animation, άμεση μετάβαση).
5. Branch `codex/sprint3-geo` (ή κλώνος `/private/tmp/gyros-sprint3` αν το .git είναι μόνο για ανάγνωση — γράψ' το στην αναφορά),
   commit ανά task, ποτέ στο main. **Καμία νέα εξάρτηση** (`topojson-client` υπάρχει ήδη: `neighbors`, `mesh`, `feature`).
6. Ύφος προς παιδιά: καθόλου πολιτική. Αμφισβητούμενες περιοχές δεν εμφανίζονται ποτέ ως γρίφος· όπου χρειάζεται, ουδέτερη διατύπωση.

## Task 0 — Δεδομένα συνόρων και συνδέσεων (θεμέλιο για όλα)
**`src/data/borders.ts`** — ελεγμένη λίστα χερσαίων συνόρων για τα 197 κράτη του dataset (iso2 → iso2[]).
- Παράγεται από `scripts/genBorders.ts` (`npm run gen:borders`) με `topojson-client` `neighbors()` πάνω στο `world-atlas/countries-50m.json`,
  και **το αποτέλεσμα γράφεται ως στατικό αρχείο TypeScript** (commit στο repo — στο runtime δεν υπολογίζεται τίποτα).
- Μετά την αυτόματη παραγωγή, **χειροκίνητες διορθώσεις** σε ξεχωριστό `BORDER_OVERRIDES` με σχόλιο ανά γραμμή. Γνωστά σημεία προς έλεγχο:
  Κόσοβο (χωρίς γεωμετρία — βάλε τα σύνορά του με το χέρι), μικροκράτη που ίσως λείπουν/ενώνονται στο 1:50m (Βατικανό, Σαν Μαρίνο, Μονακό,
  Ανδόρα, Λιχτενστάιν), γεωμετρίες μη-κρατών στο Natural Earth (Σομαλιλάνδη, Βόρεια Κύπρος, Δυτική Σαχάρα κ.λπ.) που «κόβουν» πραγματικά
  σύνορα κρατών (π.χ. Σομαλία–Αιθιοπία, Μαρόκο–Μαυριτανία): ενώνονται στο κράτος στο οποίο τα αντιμετωπίζει ο ΟΗΕ ή απλώς
  δεν εμφανίζονται ως γρίφος — τεκμηρίωσε την επιλογή, όχι πολιτική διατύπωση στο UI.
- **Ειδικά σύνορα** με σήμανση `kind: 'overseas' | 'exclave'` και ελληνική σημείωση `noteGreek` (π.χ. Γαλλία–Βραζιλία «μέσω της Γαλλικής Γουιάνας»,
  Ισπανία–Μαρόκο «μέσω Θέουτας και Μελίγιας»). Στο Εύκολο δεν ζητούνται ποτέ· στο Μεσαίο/Δύσκολο μετρούν, και η σημείωση
  εμφανίζεται μετά την απάντηση ως «Ήξερες ότι…».
- Χώρες χωρίς χερσαίους γείτονες (νησιωτικά κράτη) = κενή λίστα — είναι κανονική, έγκυρη λύση («κανένας»).
- **`src/data/links.ts`** — χειροκίνητες εκπαιδευτικές συνδέσεις για το Ταχυδρομείο: 50–80 **θαλάσσιες** (στενά και κοντινές ακτές:
  Ελλάδα–Ιταλία, Ισπανία–Μαρόκο, Γαλλία–Ηνωμένο Βασίλειο, Δανία–Σουηδία, Σρι Λάνκα–Ινδία, Μαδαγασκάρη–Μοζαμβίκη, Αυστραλία–Ινδονησία,
  Ιαπωνία–Νότια Κορέα κ.λπ.) ώστε **κάθε νησιωτικό κράτος να συνδέεται** με τουλάχιστον ένα άλλο, και 10–20 **αεροπορικές** υπεραποστάσεις
  (ήπειρος↔ήπειρος). Αυτό είναι εκπαιδευτικό δίκτυο, όχι ταξιδιωτικός οδηγός — γράψ' το και σε σχόλιο στο αρχείο.
- `test:data` επεκτείνεται: συμμετρία (αν Α συνορεύει με Β τότε και Β με Α), μόνο γνωστά iso2, ≥ 15 σημειακοί έλεγχοι
  (π.χ. Ελλάδα = Αλβανία, Βόρεια Μακεδονία, Βουλγαρία, Τουρκία· Πορτογαλία = Ισπανία· Ιαπωνία = κανένας· Κίνα = 14 γείτονες·
  Λεσότο = μόνο Νότια Αφρική· Αϊτή = Δομινικανή Δημοκρατία· Ιρλανδία = Ηνωμένο Βασίλειο), και ότι ο γράφος στεριά+θάλασσα+αέρας είναι συνεκτικός.
- Ελληνικά: όπου χρειάζεσαι όνομα χώρας σε πρόταση χρησιμοποίησε ονομαστική/αιτιατική/γενική από το dataset.

## Mode 10 — «Οι γείτονες χτυπούν την πόρτα» (`neighbors`)
- Στο κέντρο η **οικοδέσποινα**: μεγάλη CountryBall μέσα σε σπιτάκι (SVG/CSS) πάνω σε μικρό περιφερειακό χάρτη που δείχνει την ίδια και την
  περιοχή γύρω της (projection με `fitExtent` στη χώρα + περιθώριο, χωρίς ονόματα στον χάρτη πριν την απάντηση).
- Από κάτω 6 (Εύκολο) / 7 (Μεσαίο) / 8 (Δύσκολο) CountryBalls με **προσκλήσεις** (με σημαία και όνομα — εδώ η ταυτότητα των καλεσμένων
  ΔΕΝ είναι η λύση· η λύση είναι ποιοι συνορεύουν). Μείγμα από πραγματικούς γείτονες και **παγίδες**: χώρες της ίδιας περιοχής που δεν
  συνορεύουν (γείτονες-των-γειτόνων, απέναντι ακτές — π.χ. για την Ελλάδα η Ιταλία και η Κύπρος). Η σειρά ανακατεμένη.
- Ο παίκτης επιλέγει (toggle) όποιους πιστεύει ότι έχουν **χερσαία** σύνορα και πατά «Άνοιξε την πόρτα». Υπάρχει πάντα και η πρόσκληση
  «Κανένας δεν συνορεύει» — σωστή μόνο για νησιωτικά κράτη (σε αυτά ο γύρος έχει μόνο παγίδες από κοντινές χώρες).
- Κανόνας 2: πριν το «Άνοιξε την πόρτα» ο χάρτης **δεν** τονίζει σύνορα, δεν χρωματίζει γειτονικές χώρες, και στο DOM δεν υπάρχει καμία
  ένδειξη ποιοι είναι γείτονες.
- Μετά: οι σωστοί γείτονες «μπαίνουν» και κάθονται γύρω από ένα τραπέζι (happy/celebrate), τα **κοινά σύνορα φωτίζονται** στον χάρτη
  (`mesh` με φίλτρο του ζεύγους), όσοι γείτονες ξεχάστηκαν χτυπούν την πόρτα «Με ξέχασες!» (wave), οι λάθος καλεσμένοι κοιτούν την
  πρόσκληση «Μάλλον μπέρδεψα τη διεύθυνση» (shrug). Αν υπάρχει ειδικό σύνορο (overseas/exclave), «Ήξερες ότι…» με τη σημείωσή του.
- Βαθμολογία (νέα καθαρή `scoreNeighbors(correctPicked, missed, wrongPicked, streak)` με tests): τέλειος γύρος = 100 + σερί,
  αλλιώς αναλογικά, ελάχιστο 0· «σωστό» για σερί/Συλλογή μόνο ο τέλειος γύρος. Συλλογή: η οικοδέσποινα σε τέλειο γύρο.
- Επιλογή οικοδέσποινας: Εύκολο = tier 1 με 2–5 γείτονες, Μεσαίο = tier 1–2 με 1–7, Δύσκολο = όλες (και νησιωτικές, και Κίνα/Ρωσία —
  εκεί εμφανίζεται υποσύνολο με σημείωση «Βρες 5 από τους 14»).
- *Στάδιο 2 «Φτιάξε τη γειτονιά»*: **μην το υλοποιήσεις** σε αυτό το sprint· σημείωσέ το στο report με πρόταση σχεδίου.

## Mode 11 — «Το ταχυδρομείο των CountryBalls» (`post`)
- Αποστολή: «Η {αποστολέας} στέλνει δέμα στην {παραλήπτρια}». Απλοποιημένος περιφερειακός χάρτης (fitExtent στα κράτη της διαδρομής +
  γειτονιά τους) όπου **πατιούνται μόνο χώρες** (touch ≥ 44px — οι μικρές χώρες έχουν αόρατη ζώνη πατήματος γύρω από το centroid).
- Εισιτήρια ανά αποστολή, ορατά ως κουπόνια: π.χ. 🚶×3 (στεριά), ⛴️×1 (θάλασσα), ✈️×0. Ο παίκτης χτίζει τη διαδρομή πατώντας διαδοχικές
  χώρες· κάθε βήμα πρέπει να είναι σύνορο (στεριά) ή σύνδεση του `links.ts` (θάλασσα/αέρας) και καταναλώνει το αντίστοιχο εισιτήριο.
  Άκυρο βήμα → η μπάλα «ξεδιπλώνει χάρτη» μπερδεμένη (thinking) + φούσκα με τον λόγο («Δεν συνορεύουμε!»), χωρίς ποινή· κουμπί «Αναίρεση».
- «Αναχώρηση» → η μπάλα εκτελεί τη διαδρομή πάνω στον χάρτη: κυλά στη στεριά, μπαίνει σε καραβάκι στη θάλασσα, αεροπλανάκι στον αέρα
  (SVG, `projectToMap`/centroids, CSS/WAAPI — όχι JS ανά καρέ)· στον προορισμό η παραλήπτρια ανοίγει το δέμα → αυτοκόλλητο
  ή ένα σύντομο γεωγραφικό στοιχείο (`factsGreek` της παραλήπτριας ή του μεγαλύτερου σταθμού της διαδρομής).
- **Γεννήτρια γρίφων** (`src/game/postPuzzles.ts`, καθαρή, με tests): BFS στον γράφο με βάρη εισιτηρίων· κάθε αποστολή είναι **εγγυημένα
  λύσιμη** με τα δοσμένα εισιτήρια και έχει ελάχιστη διαδρομή 2–3 (Εύκολο), 3–5 (Μεσαίο), 4–7 (Δύσκολο) βήματα.
  Περιορισμοί στο Μεσαίο/Δύσκολο (ένας ανά αποστολή): «χωρίς αεροπλάνο», «πέρασε από δύο διαφορετικές ηπείρους», «όσο λιγότερες στάσεις».
- Κανόνας 2: η βέλτιστη διαδρομή **δεν** εμφανίζεται και δεν υπάρχει στο DOM πριν την «Αναχώρηση». Μετά την παράδοση: σύγκριση
  «Η διαδρομή σου: 5 στάσεις · Η πιο σύντομη: 4» με τη σύντομη διαδρομή σχεδιασμένη αχνά.
- Βαθμολογία (`scorePost(stepsUsed, minSteps, constraintMet, streak)`): επιτυχής παράδοση 100 + μπόνους 25 αν stepsUsed = minSteps +
  σερί· αποτυχία περιορισμού = μισοί πόντοι. Συλλογή: όλες οι χώρες της διαδρομής που δεν είχες.

## Mode 12 — «Ο άτλαντας έγινε κομμάτια» (`puzzle`)
- **Χειροκίνητα επιλεγμένα περιφερειακά παζλ** (`src/data/puzzles.ts`), 4–8 κομμάτια το καθένα, ≥ 12 παζλ συνολικά, π.χ.: Βαλκάνια,
  Ιβηρική + Γαλλία, Σκανδιναβία, Μπενελούξ + Γερμανία, Άλπεις, Βαλτική, Κέρας της Αφρικής, Δυτική Αφρική, Ινδοκίνα, Νότιος Κώνος,
  Κεντρική Αμερική, Μέση Ανατολή, Κεντρική Ασία, Νότια Ασία. Ποτέ παζλ 197 κομματιών.
- Κάθε κομμάτι = σιλουέτα της χώρας (ίδια projection για όλο το παζλ, `fitExtent` στην περιοχή) με τη CountryBall της να κάθεται πάνω.
  Τα κομμάτια ξεκινούν ανακατεμένα σε «δίσκο» κάτω από την περιοχή. Drag με pointer events (αφή πρώτα, `touch-action: none` μόνο στα
  κομμάτια), snap όταν το κομμάτι φτάσει εντός ανοχής της σωστής θέσης. Όσο σέρνεται, η μπάλα σπρώχνει/τραβά (nervous→proud).
- Πολύ μικρές χώρες (π.χ. Λουξεμβούργο, Μαυροβούνιο, Λίβανος, Τζιμπουτί): ελάχιστο μέγεθος πατήματος 44px και **μεγεθυμένο ένθετο**
  που δείχνει πού κουμπώνουν.
- Δυσκολία: Εύκολο = αχνό εξωτερικό περίγραμμα όλης της περιοχής + ανοχή snap μεγάλη· Μεσαίο = μόνο ακτογραμμή· Δύσκολο = μόνο μία
  χώρα-άγκυρα τοποθετημένη, όλα τα άλλα σχετικά με αυτήν.
- Κανόνας 2: οι επιμέρους θέσεις/σύνορα των χωρών **δεν** φαίνονται πριν κουμπώσουν (στο Εύκολο μόνο το εξωτερικό περίγραμμα της περιοχής,
  όπως ορίζει η ιδέα του Apollo)· στο DOM καμία συντεταγμένη-στόχος πριν το snap (κράτα τους στόχους σε closure/ref, όχι σε attributes).
- Όταν κουμπώσουν δύο γειτονικές χώρες, το κοινό τους σύνορο «ενώνεται» (λάμψη) και οι μπάλες χαιρετιούνται (wave). Ολοκλήρωση παζλ =
  celebrate όλων + ο χάρτης της περιοχής με ονόματα.
- Βαθμολογία (`scorePuzzle(pieces, misdrops, seconds, streak)`): 100 ανά παζλ + μπόνους χρόνου έως 50 − 5 ανά λάθος απόθεση (ελάχιστο 25).
  «Μήκος» μετρά παζλ (10/20 → 5/10 παζλ, ατέρμονο = όσα θέλει). Συλλογή: όλες οι χώρες του παζλ.
- «Λιγότερη κίνηση»: χωρίς πτήση κομματιών, άμεσο snap.

## Κοινά για τα τρία modes
- `GameModeId`: `'neighbors' | 'post' | 'puzzle'`, ετικέτες «Οι γείτονες χτυπούν την πόρτα», «Το ταχυδρομείο των CountryBalls»,
  «Ο άτλαντας έγινε κομμάτια»· κάρτες στο `GamesPage` (emoji 🏠 📦 🧩 + μία γραμμή περιγραφής), διαδρομές `/play/neighbors`, `/play/post`,
  `/play/puzzle`, ροή μέσω `useNewModeSession`/`NewModeLayout` όπου ταιριάζει.
- Αντιδράσεις μέσω των υπαρχόντων γεγονότων + νέα μόνο όπου χρειάζεται (π.χ. `post:depart`, `post:deliver`, `puzzle:snap`, `neighbors:open`),
  με tests στο `test:reactions`. Νέες ατάκες στο `ballLines.ts` (6–8 ανά νέα περίσταση, ολόκληρες προτάσεις).
- Νέοι ήχοι WebAudio στο `soundManager`: χτύπημα πόρτας, κουδουνάκι παράδοσης, «κλικ» κουμπώματος παζλ, σφύριγμα καραβιού.
- Βαθμολογία, πίνακας, Συλλογή, `ExplorerPassport`, `GameResults` όπως στα υπόλοιπα.
- Οι περιφερειακοί χάρτες χρησιμοποιούν την ήδη φορτωμένη τοπολογία (`getWorldTopology`) — καμία δεύτερη φόρτωση του JSON.
- Ενημέρωσε `GAME-OVERVIEW.md` (12 modes, πίνακας, κανόνες βαθμολογίας).

## QA (πριν τελειώσεις)
- `npm run build`, `npm run lint`, `npx tsc -b`, `test:data` (με borders/links), `test:engine` (γεννήτριες γρίφων + `score*`),
  `test:reactions`, `test:markup`, `audit:dist` (≤ 5 MB) καθαρά.
- `probe:offline`: πρόσθεσε `/play/neighbors`, `/play/post`, `/play/puzzle` στις οθόνες, με μία ενέργεια σε καθεμία.
- `probe:modes`: 3 γύροι ανά νέο mode σε Chrome 390×844 — ο probe βρίσκει τη λύση από τα δεδομένα (borders/links/puzzles μέσω του
  focus/seed μηχανισμού, όχι από το DOM), ελέγχει ότι **πριν τον έλεγχο το DOM δεν διαρρέει τη λύση** (κανόνας 2), παίζει έναν σωστό και
  έναν λάθος γύρο, και τραβά screenshots `tools/shots/mode-neighbors*.png`, `mode-post*.png`, `mode-puzzle*.png` (πριν/μετά).
  Για το παζλ ο probe σέρνει με `page.mouse`/touch τουλάχιστον δύο κομμάτια ως το snap.
- Αν οι browser probes μπλοκάρονται στο sandbox (Chrome SIGABRT όπως στο Sprint 2), **μην σταματήσεις**: γράψε/ενημέρωσε τους probes
  ώστε να είναι έτοιμοι και σημείωσέ το στο report — τους τρέχει ο Claude εκτός sandbox.
- `CODEX_REPORT.md`: τι έγινε ανά task, αρχεία, αποφάσεις (ιδίως για τα overrides συνόρων και τις αμφισβητούμενες γεωμετρίες), τι έμεινε.
